Write a function that determines whether a number is prime or not.
pupose:
chak if a given number is a prime number or not.
input:a singel integerm n
process:if n -< 1 not prime
check all numbers from 2 up to`n:
if any number divides n evenly not prime
otherwise -prime
output:
return True if the number is prime.
return false if the number is not prime.
example:
input7-output TRue
taha hashemi
is_prime=lambda n:False if n <=1 else
all(n%i !=0 for i in range(2,int(n**0.5)+1))
print(is_prime(7))
